//only to display info, token are separate
export class User {
    id: number;
    username: string;
    password: string;
    first_name: string;
    last_name: string;
    wiki_name: string;
    email: string;
    level: boolean;
}